{
  "name": "django-boot",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run minify",
    "serve": "vite preview",
    "minify": "esbuild var/build/*.js --minify --outdir=var/build --allow-overwrite",
    "rename": "bash ansible/rename.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "",
  "devDependencies": {
    "vite": "^5.4"
  },
  "dependencies": {
  }
}
